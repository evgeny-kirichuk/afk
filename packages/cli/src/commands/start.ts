import { defineCommand } from "citty";
export default defineCommand({
  meta: { name: "start", description: "Start an AFK session" },
  run() {
    console.log("afk start — not yet implemented");
  },
});
