import { defineCommand } from "citty";
export default defineCommand({
  meta: { name: "init", description: "Initialize AFK in this repo" },
  run() {
    console.log("afk init — not yet implemented");
  },
});
